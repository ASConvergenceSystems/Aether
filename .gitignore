# Don't commit the PDF — standard.html is the deployed version
standard.pdf

# AGS private key — keep offline, never commit
encryption_sk.json
shares/

# Operator tools — private, not part of the public spec
read_inbox.mjs

# Node
node_modules/

# OS
.DS_Store
Thumbs.db
